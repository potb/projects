{
  "name": "@potb/prout",
  "type": "module",
  "scripts": {
    "serve": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "deploy": "wrangler pages publish .svelte-kit/cloudflare --project-name prout",
    "prepare": "svelte-kit sync"
  }
}
